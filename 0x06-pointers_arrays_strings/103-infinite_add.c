#include "main.h"

/**
 * infinite_add - adss two numbers
 * @n1: First number
 * @n2: Second number
 * @r: result buffer
 * @size_r: result buffer size
 *
 * Return: 0 if the result can not be stored in r,
 * otherwise a pointer to the result
 */

char *infinite_add(char *n1, char *n2, char *r, int size_r)
